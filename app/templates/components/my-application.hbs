<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          {{#if isAdmin}}
            <li>{{#link-to "users"}}Users{{/link-to}}</li>
          {{/if}}
        <li>{{#link-to "reviews"}}Reviews{{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<h1>Match Education 360 Review</h1>

{{#if isAdmin}}
  <p>Welcome to the 360 Review Admin Portal.  From here you can create new users
  via the User menu, and view your scheduled Reviews.</p>
  {{else}}
  <p>Welcome to the 360 Review Process.  Please click on Reviews to view your
    assigned and completed reviews!</p>
{{/if}}

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>
